// using System;

// class Abundant{
	// public static void Main(string[] args){
		//Take user input
		// Console.Write("Enter a number: ");
		// int n = Convert.ToInt32(Console.ReadLine());
		
		// int sumOfDivisors = 0; //variable to store the sum of divisors
		
		// for(int i = 1; i < n; i++){
			// if(n % i == 0){
				// sumOfDivisors += i; //if the number is a divisor add it to the sum
			// }
		// }
		
		//check if number is divisible by sum of its digits
		// if(sumOfDivisors > n) Console.WriteLine("Abundant Number");
		// else Console.WriteLine("Not a Abundant Number");
		
	// }
// }
