// using System;

// class Armstrong{
	// public static void Main(string[] args){
		//Take user input
		// Console.WriteLine("Enter a number: ");
		// int n = Convert.ToInt32(Console.ReadLine());
		
		// double sum = 0;
		// int temp = n;
		
		//loop to extract each digit and add its cube to the sum variable
		// while(temp != 0){
			// int d = temp % 10; //extract last digit of the number
			// sum += Math.Pow(d, 3);
			// temp /= 10; // remove last digit
		// }
		
		//check if sum is equal to the original nunber
		// if(sum == n){
			// Console.WriteLine("Armstrong Number");
		// } else {
			// Console.WriteLine("Not an Armstrong number");
		// }
	// }
// }
