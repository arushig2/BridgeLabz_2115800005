// using System;

// class MultiplicationTableSixToNine{
	// public static void Main(string[] args){
		//take input from user
		// Console.Write("Enter a number: ");
		// int n = Convert.ToInt32(Console.ReadLine());
		// int[] res = new int[4]; //declare and array of size 4
		
		// for(int i = 0; i < res.Length; i++){ //run loop from 0 to 3
			// res[i] = n * (i + 6); //store the result for n * (i + 6) 
		// }
		
		// for(int i = 0; i < res.Length; i++){ //loop to display result
			// Console.WriteLine(n + " X " + (i + 6) + " = " + res[i]); //display the result in the desired format
		// }
		
	// }
// }