// using System;

// class SumElements{
	// public static void Main(string[] args){
		
		// double[] arr = new double[10]; //declare and array of size 10
		
		// int index = 0; //initialize index variable with 0
		// double sum = 0.0; //initialize sum variable with 0
		
		// while(true){ //run infinte loop as given in question
			// if(index == 10) break; //if index reached at 10 break out of while loop
			
			// Console.Write("Enter a number: ");
			// double n = Convert.ToDouble(Console.ReadLine()); //take input from user
			
			// if(n <= 0) break; //if user enter negative number or 0 break out of while loop
			// arr[index] = n; //if number is not negative or zero assign it to arr[index]
			// index++; //increment the index
		// }
	
		// for(int i = 0; i < index; i++){ 
			// sum += arr[i]; //sum of all the number in the array
		// }
		
		// Console.WriteLine("Sum = " + sum); //display the result 
	
		
	// }
// }